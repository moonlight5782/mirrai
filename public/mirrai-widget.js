(function () {
  "use strict";
  var script = document.currentScript;
  var scriptOrigin = script && script.src ? new URL(script.src).origin : window.location.origin;
  var defaults = { productId: "product", name: "Посмотреть товар у себя", price: "", category: "Мебель", material: "", model: "", iosModel: "", textured: "1", width: "80", height: "80", depth: "80", color: "#d2bda8", subscription: "active", label: "Посмотреть у себя", mode: "auto" };
  var allowedEvents = ["model_ready", "ar_open", "object_placed"];
  var instances = [], activeInstance = null, overlay = null, iframe = null, closeButton = null, observer = null, scanTimer = null, installationReported = {};
  function dataConfig(node) { var result = {}; if (!node || !node.dataset) return result; Object.keys(node.dataset).forEach(function (key) { result[key] = node.dataset[key]; }); return result; }
  function style(element, values) { Object.keys(values).forEach(function (key) { element.style[key] = values[key]; }); }
  function viewerUrl(config) { var url = new URL("/", scriptOrigin); var params = { widget: "1", productId: config.productId, name: config.name, price: config.price, category: config.category, material: config.material, model: config.model, iosModel: config.iosModel, textured: config.textured, width: config.width, height: config.height, depth: config.depth, color: config.color, subscription: config.subscription, parentOrigin: window.location.origin }; Object.keys(params).forEach(function (key) { if (params[key]) url.searchParams.set(key, params[key]); }); return url.toString(); }
  function ensureOverlay() {
    if (overlay) return;
    overlay = document.createElement("div"); overlay.setAttribute("role", "dialog"); overlay.setAttribute("aria-modal", "true"); overlay.setAttribute("aria-label", "AR-просмотр товара"); style(overlay, { position: "fixed", inset: "0", zIndex: "2147483647", display: "none", alignItems: "center", justifyContent: "center", padding: "24px", background: "rgba(18,18,15,.72)", backdropFilter: "blur(8px)" });
    var wrap = document.createElement("div"); style(wrap, { position: "relative", width: "min(1180px,100%)", height: "min(780px,calc(100vh - 48px))", background: "#f7f5ef", boxShadow: "0 30px 90px rgba(0,0,0,.35)" });
    closeButton = document.createElement("button"); closeButton.type = "button"; closeButton.textContent = "Закрыть ×"; closeButton.setAttribute("aria-label", "Закрыть AR-просмотр"); style(closeButton, { position: "absolute", right: "12px", top: "12px", zIndex: "2", border: "0", background: "#181814", color: "#fff", padding: "10px 13px", font: "12px Arial,sans-serif", cursor: "pointer" });
    iframe = document.createElement("iframe"); iframe.title = "MIRRAI AR"; iframe.allow = "camera; xr-spatial-tracking; fullscreen"; iframe.setAttribute("allowfullscreen", ""); style(iframe, { width: "100%", height: "100%", border: "0" });
    wrap.appendChild(closeButton); wrap.appendChild(iframe); overlay.appendChild(wrap); document.body.appendChild(overlay); closeButton.addEventListener("click", closeModal); overlay.addEventListener("click", function (event) { if (event.target === overlay) closeModal(); }); document.addEventListener("keydown", function (event) { if (event.key === "Escape" && overlay.style.display !== "none") closeModal(); });
  }
  function closeModal() { if (!overlay || !iframe) return; overlay.style.display = "none"; iframe.src = "about:blank"; document.body.style.overflow = ""; if (activeInstance) activeInstance.button.focus(); activeInstance = null; }
  function record(instance, event) { var config = instance.config; if (!config.shopId || !config.sku) return; fetch(new URL("/api/widget/events", scriptOrigin), { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ shopId: config.shopId, sku: config.sku, event: event }), keepalive: true }).catch(function () {}); }
  function openInstance(instance) { var config = instance.config, url = viewerUrl(config), mobile = /Android|iPhone|iPad|iPod/i.test(navigator.userAgent); activeInstance = instance; window.dispatchEvent(new CustomEvent("mirrai:event", { detail: { event: "widget_open", productId: config.productId } })); record(instance, "widget_open"); if (config.mode === "link" || (config.mode === "auto" && mobile)) { window.open(url, "_blank", "noopener,noreferrer"); return; } ensureOverlay(); iframe.title = "MIRRAI — " + config.name; iframe.src = url; overlay.style.display = "flex"; document.body.style.overflow = "hidden"; closeButton.focus(); }
  function createInstance(input) { var config = Object.assign({}, defaults, dataConfig(script), input || {}); var target = typeof config.target === "string" ? document.querySelector(config.target) : config.target; if (!target) target = script && script.parentElement ? script.parentElement : document.body; var button = document.createElement("button"); button.type = "button"; button.setAttribute("aria-label", config.label + ": " + config.name); button.textContent = config.label + "  ↗"; style(button, { width: "100%", minHeight: "50px", border: "0", background: "#181814", color: "#fff", padding: "14px 18px", font: "600 14px/1.2 Arial,sans-serif", letterSpacing: ".01em", cursor: "pointer" }); target.appendChild(button); var instance = { config: config, target: target, button: button }; instances.push(instance); if (config.shopId && config.sku && !config.model) button.style.display = "none"; button.addEventListener("click", function () { openInstance(instance); }); return instance; }
  function applyResult(instance, result, subscriptionActive) { if (!subscriptionActive) { instance.button.textContent = "Вы прекрасно выглядите в любой одежде"; instance.button.disabled = true; instance.button.style.display = "block"; return; } if (!result || !result.available) { instance.button.style.display = "none"; return; } instance.config = Object.assign(instance.config, result); instance.button.disabled = false; instance.button.textContent = instance.config.label + "  ↗"; instance.button.setAttribute("aria-label", instance.config.label + ": " + instance.config.name); instance.button.style.display = "block"; }
  function resolveInstances(list) { var unresolved = list.filter(function (item) { return item.config.shopId && item.config.sku && !item.config.model; }); if (!unresolved.length) return Promise.resolve(); var groups = {}; unresolved.forEach(function (item) { (groups[item.config.shopId] = groups[item.config.shopId] || []).push(item); }); return Promise.all(Object.keys(groups).map(function (shopId) { var group = groups[shopId]; return fetch(new URL("/api/widget/config", scriptOrigin), { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ shop: shopId, skus: group.map(function (item) { return item.config.sku; }) }) }).then(function (response) { return response.json(); }).then(function (result) { group.forEach(function (item) { applyResult(item, result.items && result.items[item.config.sku], result.subscriptionActive !== false); }); }).catch(function () {}); })); }
  function mount(input) { var instance = createInstance(input); resolveInstances([instance]); return { open: function () { openInstance(instance); }, close: closeModal, button: instance.button, update: function (next) { instance.config = Object.assign(instance.config, next || {}); resolveInstances([instance]); }, destroy: function () { instance.button.remove(); instances = instances.filter(function (item) { return item !== instance; }); } }; }
  function reportInstallation(shopId) { if (!shopId || installationReported[shopId]) return; installationReported[shopId] = true; fetch(new URL("/api/widget/install", scriptOrigin), { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ shopId: shopId, pageUrl: window.location.href }), keepalive: true }).catch(function () { installationReported[shopId] = false; }); }
  function scan() { var globalConfig = dataConfig(script), created = []; document.querySelectorAll("[data-mirrai-sku]").forEach(function (node) { if (node.getAttribute("data-mirrai-mounted") === "true") return; node.setAttribute("data-mirrai-mounted", "true"); created.push(createInstance(Object.assign({}, globalConfig, dataConfig(node), { target: node, shopId: node.dataset.shopId || globalConfig.shopId }))); }); resolveInstances(created); reportInstallation(globalConfig.shopId); return created.length; }
  function mountProductPage() { var globalConfig = dataConfig(script), existing = document.querySelector("[data-mirrai-auto-product]"); if (existing) return 0; var codeNode = document.querySelector(globalConfig.codeSelector || ".us-product-info-code, [itemprop=sku], .product-model"), code = codeNode && codeNode.textContent ? codeNode.textContent.trim() : ""; if (!code) { var last = decodeURIComponent(location.pathname.replace(/\/$/, "").split("/").pop() || ""), match = last.match(/(?:--?)([a-z]?\d{2,})$/i); code = match ? match[1].toUpperCase() : ""; } if (!code) return 0; var anchor = document.querySelector(globalConfig.anchorSelector || "#product, .product-info, [itemtype*='Product']"); if (!anchor || !anchor.parentNode) return 0; var target = document.createElement("div"); target.setAttribute("data-mirrai-auto-product", "true"); style(target, { marginTop: "10px", marginBottom: "10px" }); anchor.parentNode.insertBefore(target, anchor.nextSibling); var instance = createInstance(Object.assign({}, globalConfig, { target: target, shopId: globalConfig.shopId, sku: (globalConfig.skuPrefix || "") + code })); resolveInstances([instance]); reportInstallation(globalConfig.shopId); return 1; }
  function observe() { if (observer || !window.MutationObserver) return; observer = new MutationObserver(function () { clearTimeout(scanTimer); scanTimer = setTimeout(scan, 60); }); observer.observe(document.documentElement, { childList: true, subtree: true }); }
  window.addEventListener("message", function (event) { if (event.origin !== scriptOrigin || !event.data || event.data.source !== "mirrai-widget" || allowedEvents.indexOf(event.data.event) === -1 || !activeInstance) return; record(activeInstance, event.data.event); window.dispatchEvent(new CustomEvent("mirrai:event", { detail: event.data })); });
  window.MirraiWidget = { version: "1.1.0", mount: mount, scan: scan, mountProductPage: mountProductPage, close: closeModal };
  if (script && script.dataset.auto !== "false") { var start = function () { if (script.dataset.auto === "product") { mountProductPage(); } else if (script.dataset.auto === "scan" || document.querySelector("[data-mirrai-sku]")) { scan(); observe(); } else { mount(); reportInstallation(dataConfig(script).shopId); } }; if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", start, { once: true }); else start(); }
})();
